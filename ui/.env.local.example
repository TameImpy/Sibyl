# AWS credentials — use a restricted IAM role with only SQS:SendMessage + DynamoDB:GetItem/Query
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# DynamoDB table name (from CDK output: TagsTableName)
TAGS_TABLE_NAME=sibyl-content-tags-dev

# SQS queue URLs (from CDK outputs: TextQueueUrl, VideoQueueUrl)
TEXT_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/ACCOUNT_ID/sibyl-text-processing-dev
VIDEO_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/ACCOUNT_ID/sibyl-video-processing-dev
